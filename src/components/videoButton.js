import React from "react"
import styled from "styled-components"

const Ball = styled.div`
  left: ${Math.floor(Math.random() * 70)}vw;
  top: ${Math.floor(Math.random() * 70)}vw;
  color: #d1ce21;
`

const videoButton = () => {
  return <div></div>
}

export default videoButton
